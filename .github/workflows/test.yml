name: Test

on:
  workflow_call:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  test:
    name: Run test
    runs-on: ubuntu-latest
    steps:
      - run: ng test
